{
  "name": "oxplow-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/desktop dev",
    "build": "bun run --cwd apps/desktop build",
    "typecheck": "bun run --cwd apps/desktop typecheck",
    "test": "bun run --cwd apps/desktop test && cargo test --workspace",
    "test:rust": "cargo test --workspace",
    "test:ts": "bun run --cwd apps/desktop test",
    "test:collect": "cargo cov && bun run --cwd apps/desktop test:junit",
    "test:fast": "cargo nextest run",
    "test:fast:ts": "bun run --cwd apps/desktop test:junit",
    "lint:collect": "cargo clippy --workspace --all-targets --message-format=json -- -D warnings > target/clippy.json",
    "lint:searchable": "python3 scripts/check-control-chars.py",
    "type:coverage": "bunx type-coverage -p apps/desktop/tsconfig.json --json-output > target/type-coverage.json",
    "tauri:dev": "OXPLOW_PROJECT_DIR=\"$PWD\" bun run --cwd apps/desktop tauri dev",
    "tauri:build": "bun run --cwd apps/desktop tauri build",
    "tauri:build:app": "bun run --cwd apps/desktop tauri build --bundles app",
    "e2e": "bunx playwright test"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "typescript": "^5.5.0"
  }
}
