node_modules/
dist/
public/dist/
*.log
.DS_Store
.idea
*.iml
.playwright-mcp
tests-e2e.electron-archive/screenshots/
tests-e2e.electron-archive/scratch-edit-target.txt
tests-e2e.electron-archive/scratch-commit-target.txt
.self-ralph/
site/
.venv/
__pycache__/

# Dev-only OXPLOW_HOME (global app-config dir for a dev build — see DEV.md).
# Disposable: rm -rf to reset the dev instance's session/recents.
.oxplow_home/

# Rust
target/
**/target/

# Tauri-generated schemas (rebuilt by tauri-build on every cargo build)
apps/desktop/src-tauri/gen/

# Frontend build artifacts
apps/desktop/dist/

# Collection: bun JUnit test report (parsed in-place, not committed)
apps/desktop/test-report.xml

# App-level harness output (screenshots, CPU profiles) — the scripts default
# to writing these next to themselves
tests-e2e/*.png
tests-e2e/*.json
