[package]
name = "oxplow-coverage"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# Pure data types only — report parsing lives in `oxplow-collect-plugin`.
# Test-report types derive Serialize so the runtime can embed the parsed
# suite/case tree directly in an observation payload.
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
