[package]
name = "oxplow-git"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
oxplow-domain = { workspace = true }
oxplow-db = { workspace = true }
oxplow-fs-watch = { workspace = true }
# Canonical `Language` identity (the single source of truth shared across
# crates; tsk321). The AST-merge front half keys its own `MergeSpec` table
# off this enum instead of declaring a duplicate. See .context/smart-merge.md.
oxplow-code-metrics = { workspace = true }
git2 = { workspace = true }
similar = "2"
tokio = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
specta = { workspace = true }
time = { workspace = true }
# Tier-2 AST structural merge (tsk134): parse→top-level-items front half.
# tree-sitter + the first-slice grammars (rust/ts+tsx/js/python/go), plus
# the second-slice grammars (java/c/cpp/clojure, tsk137), promoted from
# dev-deps now that production code uses them.
# See .context/smart-merge.md.
tree-sitter = { workspace = true }
tree-sitter-rust = { workspace = true }
tree-sitter-typescript = { workspace = true }
tree-sitter-javascript = { workspace = true }
tree-sitter-python = { workspace = true }
tree-sitter-go = { workspace = true }
tree-sitter-java = { workspace = true }
tree-sitter-c = { workspace = true }
tree-sitter-cpp = { workspace = true }
tree-sitter-clojure-orchard = { workspace = true }

[dependencies.serde_json]
workspace = true

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true }

[lints]
workspace = true
