{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "oxplow-windows",
  "description": "Capabilities granted to the shell's own webviews — project windows (project-<n>), the launcher and the create-project confirmation. Sandboxed external-URL webviews are deliberately NOT matched here; they get the empty external-url capability instead. shell:default is replaced with a narrowly-scoped allowlist + open-url permission so the renderer can't spawn arbitrary processes.",
  "windows": ["project-*", "launcher", "setup"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "clipboard-manager:default",
    "shell:allow-open",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        { "name": "tmux", "cmd": "tmux", "args": true },
        { "name": "git", "cmd": "git", "args": true },
        {
          "name": "typescript-language-server",
          "cmd": "typescript-language-server",
          "args": true
        }
      ]
    }
  ]
}
